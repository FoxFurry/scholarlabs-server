apiVersion: v1
kind: Pod
metadata:
  name: gateway-pod
  labels:
    component: gateway
spec:
  containers:
    - name: gateway
      image: foxfurry/scholarlabs-gateway
      envFrom:
        - configMapRef:
            name: scholarlabs-gateway-${CONFIGHASH}
      ports:
        - containerPort: 8000