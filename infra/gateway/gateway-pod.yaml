apiVersion: v1
kind: Pod
metadata:
  name: gateway-pod
  labels:
    component: gateway
spec:
  containers:
    - name: gateway
      image: foxfurry/scholarlabs-gateway
      ports:
        - containerPort: 8000